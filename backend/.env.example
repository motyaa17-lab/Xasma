PORT=4000
JWT_SECRET=change_me
DATABASE_PATH=./db.sqlite
FRONTEND_ORIGIN=http://localhost:5173

# Metered TURN (optional). Domain like xasma.metered.live — no https://
# Secret from Dashboard → Developers (never commit real values).
# METERED_TURN_DOMAIN=xasma.metered.live
# METERED_TURN_SECRET=

