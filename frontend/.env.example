# Local dev (Vite). Leave empty to use smart fallbacks:
# - Desktop browser dev -> http://localhost:4000
# - Android emulator (Capacitor) -> http://10.0.2.2:4000
#
# For production builds (Vercel / APK), you MUST set this to your public backend URL:
# VITE_API_BASE=https://api.yourdomain.com
VITE_API_BASE=

