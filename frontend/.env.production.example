# Copy to .env.production before building the web app for APK.
# The app loads API + Socket.io from this base URL (HTTPS recommended).
#
# Example:
# VITE_API_BASE=https://api.yourdomain.com
#
# Without this, production builds default to http://localhost:4000 (wrong on a phone).
VITE_API_BASE=
